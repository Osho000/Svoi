spring:
  config:
    activate:
      on-profile: default
  datasource:
    url: jdbc:postgresql://localhost:5432/svoi_db
    username: postgres
    password: admin
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:postgresql://postgres:5432/svoi_db
    username: postgres
    password: admin

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
    org.springframework.web: DEBUG
    com.example.Svoi: DEBUG

jwt:
  secret: NjU0QjU2RjcxMzE0QzY4NjZBQTA2RjY2NDk4NjMwNTY=
  expiration: 86400000 # 24 часа
  header: Authorization
  prefix: Bearer